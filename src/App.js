import React from 'react'

const npm install -D tailwindcss postcss autoprefixer = () => {
  return (
	<div>npm install -D tailwindcss postcss autoprefixer</div>
  )
}

export default npm install -D tailwindcss postcss autoprefixer